apiVersion: apps/v1
kind: Deployment
metadata:
  name: smartnms-redis
  namespace: smartnms
spec:
  replicas: 1
  selector:
    matchLabels:
      app: smartnms-redis
  template:
    metadata:
      labels:
        app: smartnms-redis
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          ports:
            - containerPort: 6379
          readinessProbe:
            tcpSocket:
              port: 6379
          livenessProbe:
            tcpSocket:
              port: 6379
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: smartnms-redis
  namespace: smartnms
spec:
  selector:
    app: smartnms-redis
  ports:
    - port: 6379
      targetPort: 6379
